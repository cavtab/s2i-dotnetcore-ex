apiVersion: v1
kind: Service
metadata:
    name: "teskkluster"
    labels:
        app: "teskkluster"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "teskkluster"